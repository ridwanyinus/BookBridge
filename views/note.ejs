<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Notes</title>
    <link rel="stylesheet" href="../styles/output.css" />
  </head>
  <body class="bg-slate-950">
    <div class="flex flex-col items-center">
      <label for="book-note" class="mt-4">Note:</label>

      <form action="/add-note" method="post" class="flex flex-col gap-y-4">
        <textarea id="book-note" name="note" rows="22" cols="80" class="text-black p-0" required><%= book[0].note %></textarea>
        <button type="submit" class="bg-green-800 p-2 rounded-sm w-fit mx-auto">Submit</button>
        <input type="text" value="<%= book[0].title %>" hidden name="title" />
      </form>
    </div>
  </body>
</html>
