<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Confirm Deletion</title>
    <link rel="stylesheet" href="../styles/output.css" />
  </head>
  <body class="min-h-svh bg-[url('../images/bg.jpg')] bg-cover w-full flex flex-col items-center pt-5">
    <h1 class="heading-2 ">Are you sure you want to delete this book?</h1>
    <p class="mt-2"><strong>Title:</strong> <%= book.title %></p>
    <p class="mt-2"><strong>Author:</strong> <%= book.author %></p>

    <!-- Form to confirm deletion -->
    <form action="/delete/<%= book.title %>" method="POST" class="mt-4">
      <button type="submit" class="text-center bg-red-600 hover:bg-red-500 rounded-sm p-2 text-primary mx-auto valkyrie mr-4">Yes, Delete</button>
      <a href="/admin" class="text-center bg-slate-950 hover:bg-slate-800 rounded-sm p-2 text-primary mx-auto valkyrie">Cancel</a>
    </form>
  </body>
</html>
