<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Search New Book</title>
    <link rel="stylesheet" href="../styles/output.css" />
  </head>
  <body class="min-h-svh bg-[url('../images/bg.jpg')] bg-cover w-full flex flex-col items-center max-w-[40em]">
    <form action="/search-book" method="post">
      <label for="search" class="text-xl pt-4">Search For New Book</label>
      <div class="flex items-center">
        <input type="text" name="searchBook" id="search" class="p-1 min-w-60 text-slate-700" autocomplete="off" autofocus />
        <button type="submit" class="bg-black p-1 py-2"><img src="../images/search.svg" alt="search" /></button>
      </div>
    </form>

    <div class="mt-16">
      <h2 class="text-4xl underline valkyrie">All Result</h2>

      <div>
        <% if(locals.data){%> <% for (let i = 0; i < data.length; i++) { %>
        <a href="/admin/<%= i%>" class="border border-slate-500">
          <p><strong>Title:</strong> <%= data[i].title %></p>
          <p><strong>Author:</strong> <%= data[i].author_name ? data[i].author_name[0] : 'Unknown' %></p>

          <img src="https://covers.openlibrary.org/b/id/<%=data[i].cover_i %>-M.jpg" />
        </a>
        <% } %> <%}%>
      </div>
    </div>
  </body>
</html>
